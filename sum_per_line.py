#!/usr/bin/env python

import re
import os
import sys

input_file = sys.argv[1]
with open('input_file', 'r') as f:
    next(f)    
    read_data = f.read()

output_lines=[]

for line in read_data:
    line_list = line.split(" ")
    s = sum(line_list[1:])
    o = []
    for i in line_list 
    o.append() 
